[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "oxideflow"
version = "0.4.0"
description = "Fast catalytic descriptors for oxide surfaces, using MLIPs in place of DFT at every stage"
requires-python = ">=3.10"
dependencies = [
    "ase>=3.27",
    "pymatgen",
    "numpy",
]

[project.optional-dependencies]
dev = ["pytest"]
# Only needed to FETCH from Materials Project; cached and local files resolve without it.
mp = ["mp-api"]

[tool.setuptools.packages.find]
include = ["oxide_workflow*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
